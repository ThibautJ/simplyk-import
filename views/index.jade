extends layout

block content
	h1 SUPER-ADMIN
	.container-fluid
		.row
			div.col-md-5.col-md-1-offset
				h2 Add a school
				input#path(placeholder='folder/file name in datas folder')
				a#execute.btn.btn-default Exécute script

block footscript
	script.
		$(document).ready(function(){
			$('#execute').click(function(){
				const path = $('#path').val();
				console.log('Puet');
				console.log(Date.parse(' 2002-12-10'));
				$.get('/import_students', {path: path}, function(){
					console.log('IN GET with path : ' + path);
				});
			});
		});
